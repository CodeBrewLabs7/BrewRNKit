// __mocks__/@utils/handleLocalStorage.ts

// Mock implementation of handleLocalStorage module
const handleLocalStorage = {
  // Provide mock implementation of functions used in your component
  getItem: jest.fn(),
  setItem: jest.fn(),
  removeItem: jest.fn(),
};

export default handleLocalStorage;
